import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ArchamosFilters = () => {
  return (
    <View>
      <Text>Filters</Text>
    </View>
  );
};

export default ArchamosFilters;

const styles = StyleSheet.create({});
